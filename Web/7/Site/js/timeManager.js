// Generated by CoffeeScript 2.5.1
var showTime, timer, timerText, weekDaysRU, zeroFormat;

timer = null;

timerText = $('#timer');

weekDaysRU = ['Воскресенье', 'Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота'];

zeroFormat = function(i) {
  if (i < 10) {
    return '0' + i;
  } else {
    return i;
  }
};

showTime = function() {
  var time;
  time = new Date();
  timerText.text(zeroFormat(time.getHours()) + ':' + zeroFormat(time.getMinutes()) + ':' + zeroFormat(time.getSeconds()) + ' | ' + zeroFormat(time.getDay()) + '-' + zeroFormat(time.getMonth()) + '-' + time.getFullYear() + ' ' + weekDaysRU[time.getDay()]);
};

export var startTimeCounter = function() {
  if (!timer) {
    showTime();
    timer = setInterval(showTime, 1000);
  }
};

export var stopTimeCounter = function() {
  if (timer) {
    clearInterval(timer);
    timer = null;
  }
};

//# sourceMappingURL=timeManager.js.map
